{
  "name": "sklep-ebe-power",
  "version": "0.1.0",
  "private": true,
  "description": "ebe power — sklep z agregatami (Next.js frontend + Spring Boot backend). Root package uruchamia oba w trybie deweloperskim.",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:backend": "node dev-backend/server.mjs",
    "dev:frontend": "npm --prefix frontend run dev",
    "install:all": "npm --prefix frontend install"
  },
  "engines": {
    "node": ">=18"
  }
}
